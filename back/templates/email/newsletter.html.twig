{% extends 'base.html.twig' %}

{% block body %}
    {% for block in newspaper.blocks %}
        {% if show_block is defined and show_block%}
            <h1>
                <a
                    href="{{ path('test_mail_one', {
                        'blockType': block.type.value
                    }) }}"
                    title="Voir seulement ce bloc"
                >
                    {{ block.type.value }}
                </a>
            </h1>
        {% endif %}

        {% include 'email/block/'~block.type.value~'.html.twig' with {block:block} %}
    {% endfor %}
{% endblock %}
